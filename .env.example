# Required for `sls deploy` with Serverless Framework v4.
# Generate at https://app.serverless.com → Settings → Access Keys
SERVERLESS_ACCESS_KEY=

# Optional — only needed if you want to override defaults from the CLI.
# STAGE=dev
# AWS_REGION=ap-southeast-1
